To run this project, go to the terminal and write 'python manage.pv runserver'
this will run the webserver in port 8000.

for access, in your web browser, put the address: localhost:8000
the login is 'root'
the senha is 'root'
